[package]
name = "metastore-ui"
version = "0.1.0"
edition = "2021"
description = "UI components for metastore (skeleton)"

[dependencies]
metastore-types = { path = "../types" }
metastore-abstract = { path = "../abstract" }
