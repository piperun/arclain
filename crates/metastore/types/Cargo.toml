[package]
name = "metastore-types"
version = "0.1.0"
edition = "2021"
description = "Core types for metastore metadata library"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
