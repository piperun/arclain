[package]
name = "gameta"
version = "0.1.0"
edition = "2021"
description = "Game metadata server/client - HTTP, caching, database, API"

[dependencies]
gameta_lib = { path = "../gameta_lib" }

# Future dependencies (commented for now):
# reqwest = { version = "0.11", features = ["json"] }
# tokio = { version = "1", features = ["full"] }
# sqlx = { version = "0.7", features = ["runtime-tokio", "sqlite"] }
# axum = "0.7"
