[package]
name = "gameta_core"
version = "0.1.0"
edition = "2021"
description = "Core types and traits for gameta metadata library"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
