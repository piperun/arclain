from_latest_tag = true
ignore_merge_commits = true
branch_whitelist = ["master", "main"]
generate_mono_repository_global_tag = false
generate_mono_repository_package_tags = true

pre_bump_hooks = [
    "cargo check --workspace",
]
post_bump_hooks = [
    "cargo check --workspace",
]

[commit_types]
feature = { changelog_title = "Features" }

[changelog]
path = "CHANGELOG.md"
authors = []

# Named hook profile for full release validation
[bump_profiles.release]
pre_bump_hooks = [
    "cargo test --workspace -- --test-threads=1",
    "cargo check --workspace",
]
post_bump_hooks = [
    "cargo check --workspace",
]

# =========================================================================
# Monorepo packages
# =========================================================================

[packages.arclain_ui]
path = "crates/ui"
public_api = true
changelog_path = "crates/ui/CHANGELOG.md"

[packages.arclain_core]
path = "crates/core"
public_api = false
changelog_path = "crates/core/CHANGELOG.md"

[packages.arclain_db]
path = "crates/db"
public_api = false
changelog_path = "crates/db/CHANGELOG.md"

[packages.arclain_data]
path = "crates/data"
public_api = false
changelog_path = "crates/data/CHANGELOG.md"

[packages.arclain_plugins]
path = "crates/plugins"
public_api = false
changelog_path = "crates/plugins/CHANGELOG.md"

[packages.arclain_checksum]
path = "crates/checksum"
public_api = false
changelog_path = "crates/checksum/CHANGELOG.md"

[packages.arclain_widgets]
path = "crates/widgets"
public_api = false
changelog_path = "crates/widgets/CHANGELOG.md"

[packages.arclain_theme]
path = "crates/theme"
public_api = false
changelog_path = "crates/theme/CHANGELOG.md"

[packages.arclain-network]
path = "crates/network"
public_api = false
changelog_path = "crates/network/CHANGELOG.md"

[packages.arclain_signals]
path = "crates/signals"
public_api = false
changelog_path = "crates/signals/CHANGELOG.md"

[packages.mini-orm]
path = "crates/mini-orm"
public_api = false
changelog_path = "crates/mini-orm/CHANGELOG.md"

[packages.mini-orm-derive]
path = "crates/mini-orm/derive"
public_api = false

[packages.dlsite-metadata]
path = "plugins/dlsite-metadata"
public_api = false
changelog_path = "plugins/dlsite-metadata/CHANGELOG.md"

[packages.gstreamer-preview]
path = "plugins/gstreamer-preview"
public_api = false

[packages.ui-demo]
path = "plugins/ui-demo"
public_api = false
